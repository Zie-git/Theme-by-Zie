<div class="image-with-text" style="padding-top: {{ section.settings.padding_top }}px; padding-bottom: {{ section.settings.padding_bottom }}px;">
    {% if image_url %}
        <img src="{{ image_url }}" alt="{{ image_alt | default: 'Image Alt Text' }}" class="image">
    {% else %}
        <!-- Placeholder image URL -->
        {{ 'lifestyle-1' | placeholder_svg_tag }}
    {% endif %}
    
    <div class="text-container">
        <h2>{{ section.settings.title | escape }}</h2>
        <p>{{ section.settings.description | escape }}</p>
    </div>
</div>

{% schema %}
{
    "name": "Image",
    "settings": [
        {
            "type": "text",
            "id": "title",
            "label": "Title",
            "default": "Heading Text"
        },
        {
            "type": "textarea",
            "id": "description",
            "label": "Description",
            "default": "Description of the image and text"
        },
        {
            "type": "range",
            "id": "padding_top",
            "label": "Padding Top (rem)",
            "min": 0,
            "max": 5,
            "step": 0.1,
            "default": 1
        },
        {
            "type": "range",
            "id": "padding_bottom",
            "label": "Padding Bottom (rem)",
            "min": 0,
            "max": 5,
            "step": 0.1,
            "default": 1
        }
    ],
    "presets": [
        {
            "name": "Image"
        }
    ]
}
{% endschema %}
