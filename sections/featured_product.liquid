<div class="featured-product">
  {% if section.settings.product %}
    {% assign product = all_products[section.settings.product] %}
    <div class="product-container">
      <div class="product-image">
        <img src="{{ product.featured_image | img_url: 'medium' }}" alt="{{ product.title }}">
      </div>
      <div class="product-details">
        <h2>{{ product.title }}</h2>
        <div class="product-variants">
          <!-- Assuming you have variants, you might want to loop through them -->
          {% for variant in product.variants %}
            <label>
              <input type="radio" name="variant" value="{{ variant.id }}">
              {{ variant.title }}
            </label>
          {% endfor %}
        </div>
        <p>{{ product.description }}</p>
        <div class="product-quantity">
          <label for="quantity">Quantity:</label>
          <input type="number" id="quantity" name="quantity" min="1" value="1">
        </div>
        <button class="btn add-to-cart">Add to Cart</button>
      </div>
    </div>
  {% else %}
    <p>Please select a product in the section settings.</p>
  {% endif %}
</div>

{% schema %}
{
  "name": "Featured Product",
  "settings": [
    {
      "type": "product",
      "id": "product",
      "label": "Product",
      "info": "Select a product to feature"
    }
  ],
  "presets": [
    {
      "name": "Featured Product",
      "category": "Custom"
    }
  ]
}
{% endschema %}

<style>
.featured-product .product-container {
  display: flex;
  gap: 20px;
}

.featured-product .product-image {
  flex: 1;
}

.featured-product .product-details {
  flex: 2;
}

.featured-product .product-variants {
  margin-bottom: 10px;
}

.featured-product .product-quantity {
  margin-bottom: 10px;
}

.featured-product .btn.add-to-cart {
  display: block;
  margin-top: 10px;
}
</style>
